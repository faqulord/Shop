/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com', 'images.unsplash.com'], // Engedélyezzük külső képek betöltését
    },
}

module.exports = nextConfig